export const USER_ROLES = {
  ADMIN: 'admin',
  USER: 'user',
  GUEST: 'guest',
};

export const SUPPORTED_LANGUAGES = ['en', 'bn', 'ar'];

export const DEFAULT_LANGUAGE = 'en';

export const PAGE_SIZES = [10, 20, 30, 40];

export const DEFAULT_PAGE_SIZE = 10;
